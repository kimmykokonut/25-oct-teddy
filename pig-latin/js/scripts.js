const vowelArray = ["a", "e", "i", "o", "u"]
function igPay(text) {
        console.log(text[0]);
        console.log(vowelArray);
        if (vowelArray.includes(text[0])) {
            return true;
        }
    }

